@using Gym.WebApplication.Features.Admin.Clients.Creation.Models.Results
@using Gym.WebApplication.Features.Admin.Clients.TableView.Models.Forms

<MudDialog>
    <DialogContent>
        <MudStack Spacing="1">
            <MudAlert Severity="Severity.Info">Login: @CreateClientResult.Login</MudAlert>
            <MudAlert Severity="Severity.Info">Password: @CreateClientResult.Password</MudAlert>
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton Color="Color.Success" OnClick="@ApproveAction">Ok</MudButton>
    </DialogActions>
</MudDialog>

<style>
    .my-custom-class {
        backdrop-filter: blur(10px);
    }
</style>

@code {
    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; } = default!;

    [Parameter, EditorRequired] public CreateClientResult CreateClientResult { get; set; }

    private void ApproveAction()
    {
        MudDialog.Close();
    }
}
